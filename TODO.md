* [ ] Watch and read file `nowplaying.txt`
  * `"%artist% - %title%"` format
* [ ] Broker SSE
* [ ] HTTP server
  * SSE endpoint`/events`
* [ ] `index.html` with basic structure
  * div with `sse-swap` htmx attribute

